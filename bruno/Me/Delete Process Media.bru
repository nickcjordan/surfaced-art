meta {
  name: Delete Process Media
  type: http
  seq: 15
}

delete {
  url: {{sa_baseUrl}}/me/process-media/{{sa_processMediaId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{sa_authToken}}
}

assert {
  res.status: eq 204
}

tests {
  test("should return 204 on successful delete", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
