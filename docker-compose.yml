services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: surfaced
      POSTGRES_PASSWORD: surfaced_local
      POSTGRES_DB: surfaced
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
