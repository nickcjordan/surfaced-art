import { abbeyConfig } from './abbey-peters'
import { davidConfig } from './david-morrison'
import { karinaConfig } from './karina-yanes'

/** Real artist seed configs (ordered: Abbey is artist #1 / founding advisor). */
export const realArtistConfigs = [
  abbeyConfig,
  davidConfig,
  karinaConfig,
]
